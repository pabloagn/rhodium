# tools/color-provider/Cargo.toml

[package]
name = "color-provider"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
palette = { version = "0.7", features = ["named", "serde"] }
